import React, { Fragment } from "react";
// import styled from "styled-components";

import { List } from "../Components";

const Representatives = props => {
  return (
    <Fragment>
      <List type="Representatives" />
    </Fragment>
  );
};

// Styled components

export default Representatives;
